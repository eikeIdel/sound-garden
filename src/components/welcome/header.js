import React from "react";
import "./header.css";
import NavigationBar from "../navbar";

const Header = () => {
  return (
    <div>
      <nav>
   <NavigationBar />
      </nav>
    </div>
  );
};

export default Header;
